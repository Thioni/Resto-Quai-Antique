{% extends "base.html.twig" %}

{% block body %}

  <div class="container-fluid">

    <h1 class="text-center mb-5">Au menu</h1>

      <h2 class="text-center">Entrées</h2>

        <section class="my-5">
          <div class="row mx-auto justify-content-center">
            {% for dish in dishes %}
            {% if dish.category == "Entrée" %}
            <div class="col-md-6 col-xl-4"> 
              <div class="row text-center mb-3">
                <div class="col-12"> {{ dish.name}} </div>
                <div class="col-12"> {{ dish.description}} </div>
                <div class="col-12"> {{ dish.price}} </div>
              </div>
            </div>
            {% endif %}  
            {% endfor %}
          </div>
        </section>

      <h2 class="text-center">Plats</h2>

        <section class="my-5">
          <div class="row mx-auto justify-content-center">
            {% for dish in dishes %}
            {% if dish.category == "Plat" %}
            <div class="col-md-6 col-xl-4"> 
              <div class="row text-center mb-3">
                <div class="col-12"> {{ dish.name}} </div>
                <div class="col-12"> {{ dish.description}} </div>
                <div class="col-12"> {{ dish.price}} </div>
              </div>
            </div>
            {% endif %}  
            {% endfor %}
          </div>
        </section>

      <h2 class="text-center">Desserts</h2>

        <section class="my-5">
          <div class="row w-100 mx-auto justify-content-center">
            {% for dish in dishes %}
            {% if dish.category == "Dessert" %}
            <div class="col-md-6 col-xl-4"> 
              <div class="row text-center mb-3">
                <div class="col-12"> {{ dish.name}} </div>
                <div class="col-12"> {{ dish.description}} </div>
                <div class="col-12"> {{ dish.price}} </div>
              </div>
            </div>
            {% endif %}  
            {% endfor %}
          </div>
        </section>

      <h2 class="text-center">Vins</h2>

        <section class="my-5">
          <div class="row mx-auto justify-content-center">
            {% for dish in dishes %}
            {% if dish.category == "Vin" %}
            <div class="col-md-6 col-xl-4"> 
              <div class="row text-center mb-3">
                <div class="col-12"> {{ dish.name}} </div>
                <div class="col-12"> {{ dish.description}} </div>
                <div class="col-12"> {{ dish.price}} </div>
              </div>
            </div>
            {% endif %}  
            {% endfor %}
          </div>
        </section>

      <h2 class="text-center">Boissons</h2>

        <section class="my-5">
          <div class="row mx-auto justify-content-center">
            {% for dish in dishes %}
            {% if dish.category == "Boisson" %}
            <div class="col-md-6 col-xl-4"> 
              <div class="row text-center mb-3">
                <div class="col-12"> {{ dish.name}} </div>
                <div class="col-12"> {{ dish.description}} </div>
                <div class="col-12"> {{ dish.price}} </div>
              </div>
            </div>
            {% endif %}  
            {% endfor %}
          </div>
        </section>

  </div>


{% endblock %}